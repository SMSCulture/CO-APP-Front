import { Image } from 'expo-image';import { router } from 'expo-router';import { Pressable,ScrollView,View } from 'react-native';import { SectionHeader } from '../../../components/layout/SectionHeader';import { Text } from '../../../components/ui';import { radius,spacing } from '../../../design/tokens';import { mockCollections } from '../../../mock/collections.mock';
export function CollectionsRow(){return <View><SectionHeader title="Curated collections" spacious/><ScrollView horizontal decelerationRate="fast" showsHorizontalScrollIndicator={false} style={{marginHorizontal:-spacing.screenX}} contentContainerStyle={{paddingHorizontal:spacing.screenX,gap:spacing.md}}>{mockCollections.map(c=><Pressable key={c.id} onPress={()=>router.push(`/collections/${c.slug}`)} style={{width:278,height:190,borderRadius:radius.xl,overflow:'hidden'}}><Image source={{uri:c.imageUrl}} contentFit="cover" style={{position:'absolute',inset:0}}/><View style={{position:'absolute',inset:0,backgroundColor:'rgba(10,20,28,.42)'}}/><View style={{flex:1,justifyContent:'flex-end',padding:spacing.lg,gap:4}}><Text variant="label" color="#fff">{c.eyebrow}</Text><Text variant="heading" color="#fff">{c.title}</Text></View></Pressable>)}</ScrollView></View>}
