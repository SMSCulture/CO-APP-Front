import { ScrollView } from 'react-native'; import { PortraitEventCard } from '../../components/discovery/PortraitEventCard'; import { spacing } from '../../design/tokens'; import type { EventSummary } from '../../types/event';
/** Direct-manipulation rail: deliberate swipe, image-first cards, no automatic page changes. */
export function ThisWeekSwitcher({events}:{events:EventSummary[]}){return <ScrollView horizontal showsHorizontalScrollIndicator={false} snapToInterval={170} decelerationRate="fast" style={{marginHorizontal:-spacing.screenX}} contentContainerStyle={{paddingHorizontal:spacing.screenX,gap:spacing.md}}>{events.map(e=><PortraitEventCard key={e.id} event={e}/>)}</ScrollView>}
