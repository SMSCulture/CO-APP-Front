import type { FavoriteNode } from '../types/favorite';

/** Empty by default — mock favorites accumulate purely client-side via the store when USE_MOCK_DATA is on. */
export const mockFavoriteNodes: FavoriteNode[] = [];
